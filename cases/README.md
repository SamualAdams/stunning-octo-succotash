# Valuation Cases

The app stores saved valuation cases in this directory as JSON files.

Folders in the sidebar map to real directories here, and each case is one `.json` file. Keeping cases as files makes changes easy to inspect, diff, and commit when you want a tracked history.
